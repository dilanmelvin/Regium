# Changesets

This folder contains [Changesets](https://github.com/changesets/changesets) used to generate version bumps and changelogs.

To add a changeset:

```bash
pnpm changeset
```

Pick the affected packages and the severity (patch / minor / major).
